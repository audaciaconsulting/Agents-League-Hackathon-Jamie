---
description: "Move Steam to the top of the platform connectors list"
---

## Prompt

In the platform connectors section, can you move the steam section so the top,
above the Xbox section?

## Response

Moved Steam to the first position in the source-status order used by the
analysis payload so the connector list now renders Steam above Xbox.

Also updated the client-side placeholder render so the initial dashboard state
matches the analyzed ordering before any request runs.