# Future Work

- Update the script in `scripts/` so it creates the full set of Foundry
  resources end to end.
- Include any missing provisioning steps, dependency wiring, and validation
  needed for a fresh deployment.
